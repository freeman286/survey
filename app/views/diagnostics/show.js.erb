<% if @segment %>
	$("#questions").html('<%= escape_javascript(render 'segments/questions', :@segment => @segment)%>');
	$("#wheel").transition({ rotate: '<%= @segment.segment_roation %>deg' });
<% end %>
