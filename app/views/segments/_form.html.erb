<%= simple_form_for(@segment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :diagnostic %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
